use std::{cell::RefCell, collections::HashMap, env, fs, path::{Path, PathBuf}, process, rc::Rc, thread, time::{SystemTime, UNIX_EPOCH, Duration}};

#[derive(Clone, Debug, PartialEq)]
enum Token {
    Name(String), Number(i64), Text(String), Plus, Minus, Star, Slash, Percent,
    EqEq, NotEq, Less, Greater, LessEq, GreaterEq, And, Or, LParen, RParen,
    LBracket, RBracket, LBrace, RBrace, Colon, Comma, Dot, End,
}

fn tokenize(source: &str) -> Result<Vec<Token>, String> {
    let chars: Vec<char> = source.chars().collect(); let mut i = 0; let mut out = Vec::new();
    while i < chars.len() {
        let c = chars[i];
        if c.is_whitespace() { i += 1; continue; }
        if c == '#' { while i < chars.len() && chars[i] != '\n' { i += 1; } continue; }
        if c.is_ascii_digit() { let s=i; while i<chars.len() && chars[i].is_ascii_digit(){i+=1;} out.push(Token::Number(chars[s..i].iter().collect::<String>().parse().unwrap())); continue; }
        if c.is_ascii_alphabetic() || c == '_' { let s=i; while i<chars.len() && (chars[i].is_ascii_alphanumeric() || chars[i]=='_'){i+=1;} let word: String = chars[s..i].iter().collect(); out.push(match word.as_str() { "and" => Token::And, "or" => Token::Or, _ => Token::Name(word) }); continue; }
        if c == '"' { i+=1; let mut s=String::new(); while i<chars.len() && chars[i]!='"' { if chars[i]=='\\' && i+1<chars.len(){ i+=1; s.push(match chars[i]{'n'=>'\n','t'=>'\t','"'=>'"','\\'=>'\\',x=>x}); } else {s.push(chars[i]);} i+=1; } if i==chars.len(){return Err("unterminated string".into());} i+=1; out.push(Token::Text(s)); continue; }
        let t=match c {'+'=>Token::Plus,'-'=>Token::Minus,'*'=>Token::Star,'/'=>Token::Slash,'%'=>Token::Percent,'('=>Token::LParen,')'=>Token::RParen,'['=>Token::LBracket,']'=>Token::RBracket,'{'=>Token::LBrace,'}'=>Token::RBrace,':'=>Token::Colon,','=>Token::Comma,'=' if i+1<chars.len()&&chars[i+1]=='='=>{i+=1;Token::EqEq},'!' if i+1<chars.len()&&chars[i+1]=='='=>{i+=1;Token::NotEq},'<' if i+1<chars.len()&&chars[i+1]=='='=>{i+=1;Token::LessEq},'>' if i+1<chars.len()&&chars[i+1]=='='=>{i+=1;Token::GreaterEq},'<'=>Token::Less,'>'=>Token::Greater,'.'=>Token::Dot,_=>return Err(format!("unexpected character: {c}"))}; out.push(t); i+=1;
    }
    out.push(Token::End); Ok(out)
}

#[derive(Clone, Debug, PartialEq)]
struct Function { params: Vec<String>, body: Vec<String>, closure: HashMap<String,Value> }
#[derive(Clone, Debug, PartialEq)]
enum Value { Text(String), Number(i64), Bool(bool), List(Vec<Value>), Map(HashMap<String,Value>), Object { class_name: String, fields: Rc<RefCell<HashMap<String,Value>>> }, None }
impl Value {
    fn show(&self)->String { match self { Self::Text(x)=>x.clone(),Self::Number(x)=>x.to_string(),Self::Bool(x)=>x.to_string(),Self::List(x)=>format!("[{}]",x.iter().map(Self::show).collect::<Vec<_>>().join(", ")),Self::Map(x)=>format!("{{{} keys}}",x.len()),Self::Object{class_name,..}=>format!("<object {class_name}>"),Self::None=>"none".into() } }
    fn truthy(&self)->bool { match self {Self::Bool(x)=>*x,Self::Number(x)=>*x!=0,Self::Text(x)=>!x.is_empty(),Self::List(x)=>!x.is_empty(),Self::Map(x)=>!x.is_empty(),Self::Object{..}=>true,Self::None=>false} }
}

enum Flow { Continue, Break, LoopContinue, Return(Value) }
struct ExprParser<'a> { tokens:&'a [Token], pos:usize, vars:&'a HashMap<String,Value>, funcs:&'a HashMap<String,Rc<Function>> }
impl<'a> ExprParser<'a> {
    fn new(t:&'a [Token],v:&'a HashMap<String,Value>,f:&'a HashMap<String,Rc<Function>>)->Self{Self{tokens:t,pos:0,vars:v,funcs:f}}
    fn call_args(&mut self)->Result<Vec<Value>,String>{let mut args=Vec::new();if *self.peek()!=Token::RParen{loop{args.push(self.parse(0)?);if *self.peek()==Token::RParen{break;}if self.take()!=Token::Comma{return Err("expected comma in call".into());}}}if self.take()!=Token::RParen{return Err("expected ) after call".into());}Ok(args)}
    fn peek(&self)->&Token{&self.tokens[self.pos]}
    fn take(&mut self)->Token{let x=self.tokens[self.pos].clone();self.pos+=1;x}
    fn parse(&mut self,min:u8)->Result<Value,String>{
        let mut left=match self.take(){
            Token::Number(n)=>Value::Number(n),Token::Text(s)=>Value::Text(s),Token::Minus=>match self.parse(4)?{Value::Number(n)=>Value::Number(-n),_=>return Err("unary - expects a number".into())},
            Token::Name(n) if n=="true"=>Value::Bool(true),Token::Name(n) if n=="false"=>Value::Bool(false),Token::Name(n) if n=="not"=>Value::Bool(!self.parse(4)?.truthy()),Token::Name(n) if n=="none"=>Value::None,
            Token::Name(n) if n=="len" && *self.peek()==Token::LParen=>{self.take();let v=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after len".into());}match v{Value::List(x)=>Value::Number(x.len() as i64),Value::Text(x)=>Value::Number(x.chars().count() as i64),_=>return Err("len expects a list or text".into())}},
            Token::Name(n) if n=="str" && *self.peek()==Token::LParen=>{self.take();let v=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after str".into());}Value::Text(v.show())},
            Token::Name(n) if n=="now" && *self.peek()==Token::LParen=>{self.take();if self.take()!=Token::RParen{return Err("expected ) after now".into());}Value::Number(SystemTime::now().duration_since(UNIX_EPOCH).map_err(|_|"system clock is before Unix epoch".to_string())?.as_secs() as i64)},
            Token::Name(n) if n=="sleep" && *self.peek()==Token::LParen=>{self.take();let v=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after sleep".into());}match v{Value::Number(ms) if ms>=0=>{thread::sleep(Duration::from_millis(ms as u64));Value::None},_=>return Err("sleep expects a non-negative number of milliseconds".into())}},
            Token::Name(n) if n=="env" && *self.peek()==Token::LParen=>{self.take();let key=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after env".into());}match key{Value::Text(k)=>Value::Text(env::var(k).unwrap_or_default()),_=>return Err("env expects a text key".into())}},
            Token::Name(n) if n=="has_env" && *self.peek()==Token::LParen=>{self.take();let key=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after has_env".into());}match key{Value::Text(k)=>Value::Bool(env::var_os(k).is_some()),_=>return Err("has_env expects a text key".into())}},
            Token::Name(n) if n=="exists" && *self.peek()==Token::LParen=>{self.take();let path=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after exists".into());}match path{Value::Text(p)=>Value::Bool(Path::new(&p).exists()),_=>return Err("exists expects a text path".into())}},
            Token::Name(n) if n=="path_join" && *self.peek()==Token::LParen=>{self.take();let mut parts=Vec::new();if *self.peek()!=Token::RParen{loop{parts.push(match self.parse(0)?{Value::Text(p)=>p,_=>return Err("path_join expects text parts".into())});if *self.peek()==Token::RParen{break;}if self.take()!=Token::Comma{return Err("expected comma in path_join".into());}}}self.take();Value::Text(parts.into_iter().fold(PathBuf::new(),|mut path,part|{path.push(part);path}).to_string_lossy().into())},
            Token::Name(n) if n=="basename" && *self.peek()==Token::LParen=>{self.take();let path=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after basename".into());}match path{Value::Text(p)=>Value::Text(Path::new(&p).file_name().and_then(|x|x.to_str()).unwrap_or("").into()),_=>return Err("basename expects a text path".into())}},
            Token::Name(n) if n=="dirname" && *self.peek()==Token::LParen=>{self.take();let path=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after dirname".into());}match path{Value::Text(p)=>Value::Text(Path::new(&p).parent().and_then(|x|x.to_str()).unwrap_or("").into()),_=>return Err("dirname expects a text path".into())}},
            Token::Name(n) if n=="pow" && *self.peek()==Token::LParen=>{self.take();let base=self.parse(0)?;if self.take()!=Token::Comma{return Err("expected comma in pow".into());}let exponent=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after pow".into());}match(base,exponent){(Value::Number(a),Value::Number(b)) if b>=0=>Value::Number(a.checked_pow(b as u32).ok_or("pow result overflow".to_string())?),_=>return Err("pow expects a number and a non-negative exponent".into())}},
            Token::Name(n) if n=="sqrt" && *self.peek()==Token::LParen=>{self.take();let value=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after sqrt".into());}match value{Value::Number(x) if x>=0=>Value::Number((x as f64).sqrt().round() as i64),_=>return Err("sqrt expects a non-negative number".into())}},
            Token::Name(n) if n=="abs" && *self.peek()==Token::LParen=>{self.take();let v=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after abs".into());}match v{Value::Number(x)=>Value::Number(x.abs()),_=>return Err("abs expects a number".into())}},
            Token::Name(n) if n=="min" && *self.peek()==Token::LParen=>{self.take();let a=self.parse(0)?;if self.take()!=Token::Comma{return Err("expected comma in min".into());}let b=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after min".into());}match(a,b){(Value::Number(x),Value::Number(y))=>Value::Number(x.min(y)),_=>return Err("min expects numbers".into())}},
            Token::Name(n) if n=="max" && *self.peek()==Token::LParen=>{self.take();let a=self.parse(0)?;if self.take()!=Token::Comma{return Err("expected comma in max".into());}let b=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after max".into());}match(a,b){(Value::Number(x),Value::Number(y))=>Value::Number(x.max(y)),_=>return Err("max expects numbers".into())}},
            Token::Name(n) if n=="upper" && *self.peek()==Token::LParen=>{self.take();let v=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after upper".into());}match v{Value::Text(x)=>Value::Text(x.to_uppercase()),_=>return Err("upper expects text".into())}},
            Token::Name(n) if n=="lower" && *self.peek()==Token::LParen=>{self.take();let v=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after lower".into());}match v{Value::Text(x)=>Value::Text(x.to_lowercase()),_=>return Err("lower expects text".into())}},
            Token::Name(n) if n=="trim" && *self.peek()==Token::LParen=>{self.take();let v=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after trim".into());}match v{Value::Text(x)=>Value::Text(x.trim().into()),_=>return Err("trim expects text".into())}},
            Token::Name(n) if n=="split" && *self.peek()==Token::LParen=>{self.take();let text=self.parse(0)?;if self.take()!=Token::Comma{return Err("expected comma in split".into());}let separator=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after split".into());}match(text,separator){(Value::Text(x),Value::Text(sep))=>Value::List(x.split(&sep).map(|part|Value::Text(part.into())).collect()),_=>return Err("split expects text and separator".into())}},
            Token::Name(n) if n=="type" && *self.peek()==Token::LParen=>{self.take();let v=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after type".into());}Value::Text(match v{Value::Text(_)=>"text",Value::Number(_)=>"number",Value::Bool(_)=>"bool",Value::List(_)=>"list",Value::Map(_)=>"map",Value::Object{..}=>"object",Value::None=>"none"}.into())},
            Token::Name(n) if n=="keys" && *self.peek()==Token::LParen=>{self.take();let v=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after keys".into());}match v{Value::Map(m)=>Value::List(m.keys().cloned().map(Value::Text).collect()),_=>return Err("keys expects a map".into())}},
            Token::Name(n) if n=="contains" && *self.peek()==Token::LParen=>{self.take();let collection=self.parse(0)?;if self.take()!=Token::Comma{return Err("expected comma in contains".into());}let item=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after contains".into());}Value::Bool(match collection{Value::List(xs)=>xs.iter().any(|x|*x==item),Value::Text(s)=>match item{Value::Text(q)=>s.contains(&q),_=>false},Value::Map(m)=>match item{Value::Text(k)=>m.contains_key(&k),_=>false},_=>false})},
            Token::Name(n) if n=="join" && *self.peek()==Token::LParen=>{self.take();let values=self.parse(0)?;if self.take()!=Token::Comma{return Err("expected comma in join".into());}let separator=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after join".into());}match(values,separator){(Value::List(xs),Value::Text(sep))=>Value::Text(xs.iter().map(Value::show).collect::<Vec<_>>().join(&sep)),_=>return Err("join expects a list and text separator".into())}},
            Token::Name(n) if n=="is_empty" && *self.peek()==Token::LParen=>{self.take();let v=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after is_empty".into());}Value::Bool(match v{Value::Text(x)=>x.is_empty(),Value::List(x)=>x.is_empty(),Value::Map(x)=>x.is_empty(),Value::None=>true,_=>false})},
            Token::Name(n) if n=="sum" && *self.peek()==Token::LParen=>{self.take();let v=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after sum".into());}match v{Value::List(xs)=>{let mut total=0i64;for item in xs{if let Value::Number(n)=item{total=total.checked_add(n).ok_or("sum overflow".to_string())?;}else{return Err("sum expects a list of numbers".into());}}Value::Number(total)},_=>return Err("sum expects a list".into())}},
            Token::Name(n) if n=="reverse" && *self.peek()==Token::LParen=>{self.take();let v=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after reverse".into());}match v{Value::Text(x)=>Value::Text(x.chars().rev().collect()),Value::List(mut x)=>{x.reverse();Value::List(x)},_=>return Err("reverse expects text or list".into())}},
            Token::Name(n) if n=="sort" && *self.peek()==Token::LParen=>{self.take();let v=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after sort".into());}match v{Value::List(mut x)=>{if x.iter().all(|v|matches!(v,Value::Number(_))){x.sort_by_key(|v|if let Value::Number(n)=v{*n}else{0});}else if x.iter().all(|v|matches!(v,Value::Text(_))){x.sort_by_key(|v|if let Value::Text(s)=v{s.clone()}else{String::new()});}else{return Err("sort expects a list of numbers or text".into());}Value::List(x)},_=>return Err("sort expects a list".into())}},
            Token::Name(n) if n=="get" && *self.peek()==Token::LParen=>{self.take();let map=self.parse(0)?;if self.take()!=Token::Comma{return Err("expected comma in get".into());}let key=self.parse(0)?;if self.take()!=Token::Comma{return Err("expected default value in get".into());}let default=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after get".into());}match(map,key){(Value::Map(m),Value::Text(k))=>m.get(&k).cloned().unwrap_or(default),_=>return Err("get expects a map, text key, and default value".into())}},
            Token::Name(n) if n=="assert" && *self.peek()==Token::LParen=>{self.take();let condition=self.parse(0)?;let message=if *self.peek()==Token::Comma{self.take();self.parse(0)?}else{Value::Text("assertion failed".into())};if self.take()!=Token::RParen{return Err("expected ) after assert".into());}if condition.truthy(){Value::None}else{return Err(message.show())}},
            Token::Name(n) if n=="json" && *self.peek()==Token::LParen=>{self.take();let v=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after json".into());}Value::Text(serde_json::to_string(&value_to_json(&v)).map_err(|e|format!("json encode failed: {e}"))?)},
            Token::Name(n) if n=="from_json" && *self.peek()==Token::LParen=>{self.take();let v=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after from_json".into());}match v{Value::Text(s)=>json_to_value(serde_json::from_str(&s).map_err(|e|format!("from_json failed: {e}"))?),_=>return Err("from_json expects text".into())}},
            Token::Name(n) if n=="new" && *self.peek()==Token::LParen=>{self.take();let class=self.parse(0)?;let mut ctor_args=Vec::new();let mut fields=HashMap::new();while *self.peek()==Token::Comma{self.take();let value=self.parse(0)?;if ctor_args.is_empty(){if let Value::Map(m)=value{fields=m;continue;}}ctor_args.push(value);}if self.take()!=Token::RParen{return Err("expected ) after new".into());}match class{Value::Text(class_name)=>{if !self.funcs.contains_key(&format!("{class_name}.__class__")){return Err(format!("unknown class: {class_name}"));}let object=Value::Object{class_name:class_name.clone(),fields:Rc::new(RefCell::new(fields))};if self.funcs.contains_key(&format!("{class_name}.__own_init__")){if let Some(parent_meta)=self.funcs.get(&format!("{class_name}.__parent__")){if let Some(parent_name)=parent_meta.body.first(){if let Some(parent_init)=self.funcs.get(&format!("{parent_name}.init")).cloned(){call_method(&parent_init,ctor_args.clone(),object.clone(),self.funcs)?;}}}}if let Some(init)=self.funcs.get(&format!("{class_name}.init")).cloned(){call_method(&init,ctor_args,object.clone(),self.funcs)?;}object},_=>return Err("new expects a text class name".into())}},
            Token::Name(n) if n=="range" && *self.peek()==Token::LParen=>{self.take();let first=self.parse(0)?;let (start,end)=if *self.peek()==Token::Comma{self.take();(first,self.parse(0)?)}else{(Value::Number(0),first)};if self.take()!=Token::RParen{return Err("expected ) after range".into());}match(start,end){(Value::Number(a),Value::Number(b)) if a<=b=>Value::List((a..b).map(Value::Number).collect()),_=>return Err("range expects numeric bounds".into())}},
            Token::Name(n) if n=="read_text" && *self.peek()==Token::LParen=>{self.take();let path=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after read_text".into());}match path{Value::Text(p)=>Value::Text(fs::read_to_string(p).map_err(|e|format!("read_text failed: {e}"))?),_=>return Err("read_text expects a text path".into())}},
            Token::Name(n) if n=="write_text" && *self.peek()==Token::LParen=>{self.take();let path=self.parse(0)?;if self.take()!=Token::Comma{return Err("expected comma in write_text".into());}let content=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after write_text".into());}match(path,content){(Value::Text(p),Value::Text(c))=>{fs::write(p,c).map_err(|e|format!("write_text failed: {e}"))?;Value::None},_=>return Err("write_text expects text path and content".into())}},
            Token::Name(n) if n=="read_lines" && *self.peek()==Token::LParen=>{self.take();let path=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after read_lines".into());}match path{Value::Text(p)=>Value::List(fs::read_to_string(p).map_err(|e|format!("read_lines failed: {e}"))?.lines().map(|line|Value::Text(line.into())).collect()),_=>return Err("read_lines expects a text path".into())}},
            Token::Name(n) if n=="write_lines" && *self.peek()==Token::LParen=>{self.take();let path=self.parse(0)?;if self.take()!=Token::Comma{return Err("expected comma in write_lines".into());}let lines=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected ) after write_lines".into());}match(path,lines){(Value::Text(p),Value::List(xs))=>{let mut out=String::new();for (i,v) in xs.iter().enumerate(){if i>0{out.push('\n');}if let Value::Text(s)=v{out.push_str(s)}else{return Err("write_lines expects a list of text".into())}}fs::write(p,out).map_err(|e|format!("write_lines failed: {e}"))?;Value::None},_=>return Err("write_lines expects a text path and list".into())}},
            Token::Name(n) if *self.peek()==Token::LParen=>{self.take();let mut args=Vec::new();if *self.peek()!=Token::RParen{loop{args.push(self.parse(0)?);if *self.peek()==Token::RParen{break;}if self.take()!=Token::Comma{return Err("expected comma in call".into());}}}self.take();let f=self.funcs.get(&n).ok_or(format!("undefined function: {n}"))?.clone();call_function(&f,args,self.funcs)?},
            Token::Name(n)=>self.vars.get(&n).cloned().ok_or(format!("undefined variable: {n}"))?,
            Token::LParen=>{let v=self.parse(0)?;if self.take()!=Token::RParen{return Err("expected )".into());}v},
            Token::LBracket=>{let mut x=Vec::new();if *self.peek()!=Token::RBracket{loop{x.push(self.parse(0)?);if *self.peek()==Token::RBracket{break;}if self.take()!=Token::Comma{return Err("expected comma".into());}}}self.take();Value::List(x)},
            Token::LBrace=>{let mut m=HashMap::new();if *self.peek()!=Token::RBrace{loop{let k=match self.take(){Token::Text(s)|Token::Name(s)=>s,_=>return Err("map key must be text or name".into())};if self.take()!=Token::Colon{return Err("expected : in map".into());}m.insert(k,self.parse(0)?);if *self.peek()==Token::RBrace{break;}if self.take()!=Token::Comma{return Err("expected comma in map".into());}}}self.take();Value::Map(m)},
            x=>return Err(format!("unexpected token: {x:?}")),
        };
        loop { if *self.peek()==Token::Dot{self.take();let member=match self.take(){Token::Name(name)=>name,_=>return Err("expected a property or method name after .".into())};if *self.peek()==Token::LParen{self.take();let args=self.call_args()?;let class_name=match &left{Value::Object{class_name,..}=>class_name.clone(),_=>return Err("methods can only be called on objects".into())};let f=self.funcs.get(&format!("{class_name}.{member}")).ok_or(format!("undefined method: {class_name}.{member}"))?.clone();left=call_method(&f,args,left,self.funcs)?;}else{left=match left{Value::Object{fields,..}=>fields.borrow().get(&member).cloned().ok_or(format!("property not found: {member}"))?,Value::Map(m)=>m.get(&member).cloned().ok_or(format!("key not found: {member}"))?,_=>return Err("property access expects an object or map".into())};}continue;} if *self.peek()==Token::LBracket{self.take();let idx=self.parse(0)?;if self.take()!=Token::RBracket{return Err("expected ]".into());}left=match(left,idx){(Value::List(x),Value::Number(n)) if n>=0=>x.get(n as usize).cloned().ok_or("index out of range".to_string())?,(Value::Map(m),Value::Text(k))=>m.get(&k).cloned().ok_or("key not found".to_string())?,_=>return Err("invalid index operation".into())};continue;}let op=self.peek().clone();let p=match op{Token::EqEq|Token::NotEq|Token::Less|Token::Greater|Token::LessEq|Token::GreaterEq|Token::And|Token::Or=>1,Token::Plus|Token::Minus=>2,Token::Star|Token::Slash|Token::Percent=>3,_=>0};if p==0||p<min{break;}self.take();left=operate(left,op,self.parse(p+1)?)?;}Ok(left)
    }
}
fn operate(a:Value,op:Token,b:Value)->Result<Value,String>{match(a,op,b){(Value::Number(x),Token::Plus,Value::Number(y))=>Ok(Value::Number(x+y)),(Value::Number(x),Token::Minus,Value::Number(y))=>Ok(Value::Number(x-y)),(Value::Number(x),Token::Star,Value::Number(y))=>Ok(Value::Number(x*y)),(Value::Number(_),Token::Slash,Value::Number(0))=>Err("division by zero".into()),(Value::Number(x),Token::Slash,Value::Number(y))=>Ok(Value::Number(x/y)),(Value::Number(x),Token::Percent,Value::Number(y))=>Ok(Value::Number(x%y)),(Value::Text(x),Token::Plus,Value::Text(y))=>Ok(Value::Text(x+&y)),(Value::Bool(x),Token::And,Value::Bool(y))=>Ok(Value::Bool(x&&y)),(Value::Bool(x),Token::Or,Value::Bool(y))=>Ok(Value::Bool(x||y)),(x,Token::EqEq,y)=>Ok(Value::Bool(x==y)),(x,Token::NotEq,y)=>Ok(Value::Bool(x!=y)),(Value::Number(x),Token::Less,Value::Number(y))=>Ok(Value::Bool(x<y)),(Value::Number(x),Token::Greater,Value::Number(y))=>Ok(Value::Bool(x>y)),(Value::Number(x),Token::LessEq,Value::Number(y))=>Ok(Value::Bool(x<=y)),(Value::Number(x),Token::GreaterEq,Value::Number(y))=>Ok(Value::Bool(x>=y)),_=>Err("invalid operation".into())}}
fn value_to_json(v:&Value)->serde_json::Value{match v{Value::None=>serde_json::Value::Null,Value::Bool(x)=>serde_json::Value::Bool(*x),Value::Number(x)=>serde_json::Value::Number((*x).into()),Value::Text(x)=>serde_json::Value::String(x.clone()),Value::List(xs)=>serde_json::Value::Array(xs.iter().map(value_to_json).collect()),Value::Map(m)=>serde_json::Value::Object(m.iter().map(|(k,v)|(k.clone(),value_to_json(v))).collect()),Value::Object{class_name,fields}=>{let mut object=serde_json::Map::new();object.insert("__class".into(),serde_json::Value::String(class_name.clone()));for(k,v)in fields.borrow().iter(){object.insert(k.clone(),value_to_json(v));}serde_json::Value::Object(object)}}}
fn json_to_value(v:serde_json::Value)->Value{match v{serde_json::Value::Null=>Value::None,serde_json::Value::Bool(x)=>Value::Bool(x),serde_json::Value::Number(x)=>Value::Number(x.as_i64().unwrap_or(0)),serde_json::Value::String(x)=>Value::Text(x),serde_json::Value::Array(xs)=>Value::List(xs.into_iter().map(json_to_value).collect()),serde_json::Value::Object(m)=>Value::Map(m.into_iter().map(|(k,v)|(k,json_to_value(v))).collect())}}
fn expression(raw:&str,vars:&HashMap<String,Value>,funcs:&HashMap<String,Rc<Function>>)->Result<Value,String>{ExprParser::new(&tokenize(raw)?,vars,funcs).parse(0)}
fn indented(lines:&[String],start:usize)->(Vec<String>,usize){let mut i=start;let mut body=Vec::new();while i<lines.len()&&(lines[i].starts_with(' ')||lines[i].starts_with('\t')){let line=&lines[i];let normalized=if line.starts_with('\t'){line[1..].to_string()}else{line.strip_prefix("    ").unwrap_or(line).to_string()};body.push(normalized);i+=1;}(body,i)}
fn call_function(f:&Function,args:Vec<Value>,funcs:&HashMap<String,Rc<Function>>)->Result<Value,String>{if args.len()!=f.params.len(){return Err(format!("function expects {} arguments, got {}",f.params.len(),args.len()));}let mut local=f.closure.clone();for(k,v)in f.params.iter().zip(args){local.insert(k.clone(),v);}let mut local_funcs=funcs.clone();match execute_lines(&f.body,&mut local,&mut local_funcs,Path::new("."))?{Flow::Return(v)=>Ok(v),Flow::Continue=>Ok(Value::None),Flow::Break|Flow::LoopContinue=>Err("break/continue cannot be used outside a loop".into())}}
fn call_method(f:&Function,args:Vec<Value>,self_value:Value,funcs:&HashMap<String,Rc<Function>>)->Result<Value,String>{if args.len()+1!=f.params.len(){return Err(format!("method expects {} arguments after self, got {}",f.params.len().saturating_sub(1),args.len()));}let mut local=f.closure.clone();local.insert("self".into(),self_value);for(k,v)in f.params.iter().skip(1).zip(args){local.insert(k.clone(),v);}let mut local_funcs=funcs.clone();match execute_lines(&f.body,&mut local,&mut local_funcs,Path::new("."))?{Flow::Return(v)=>Ok(v),Flow::Continue=>Ok(Value::None),Flow::Break|Flow::LoopContinue=>Err("break/continue cannot be used outside a loop".into())}}
fn value_type(v:&Value)->&'static str{match v{Value::Text(_)=>"text",Value::Number(_)=>"number",Value::Bool(_)=>"bool",Value::List(_)=>"list",Value::Map(_)=>"map",Value::Object{..}=>"object",Value::None=>"none"}}
fn check_annotation(name:&str,annotation:&str,value:&Value)->Result<(),String>{let expected=annotation.trim();if expected.is_empty(){return Ok(());}let valid=matches!((expected,value_type(value)),("text","text")|("number","number")|("bool","bool")|("list","list")|("map","map")|("none","none")|("any",_));if valid{Ok(())}else{Err(format!("type mismatch for {name}: expected {expected}, got {}",value_type(value)))}}
fn execute_lines(lines:&[String],vars:&mut HashMap<String,Value>,funcs:&mut HashMap<String,Rc<Function>>,base:&Path)->Result<Flow,String>{let mut i=0;while i<lines.len(){let line=lines[i].trim();if line.is_empty()||line.starts_with('#'){i+=1;continue;}
    if let Some(rest)=line.strip_prefix("class "){let head=rest.trim_end_matches(':').trim();let mut parts=head.split_whitespace();let class_name=parts.next().ok_or("class syntax: class Name:".to_string())?.to_string();let parent=if parts.next()==Some("extends"){parts.next().map(str::to_string)}else{None};funcs.insert(format!("{class_name}.__class__"),Rc::new(Function{params:Vec::new(),body:Vec::new(),closure:vars.clone()}));if let Some(parent_name)=parent.clone(){if !funcs.contains_key(&format!("{parent_name}.__class__")){return Err(format!("unknown parent class: {parent_name}"));}funcs.insert(format!("{class_name}.__parent__"),Rc::new(Function{params:Vec::new(),body:vec![parent_name],closure:vars.clone()}));}let(body,end)=indented(lines,i+1);let mut j=0;while j<body.len(){let method_line=body[j].trim();if let Some(method_rest)=method_line.strip_prefix("fn ").or_else(||method_line.strip_prefix("def ")){let method_head=method_rest.trim_end_matches(':');let(method_name,args)=method_head.split_once('(').ok_or("method syntax: fn name(self):".to_string())?;if method_name.trim()=="init"{funcs.insert(format!("{class_name}.__own_init__"),Rc::new(Function{params:Vec::new(),body:Vec::new(),closure:vars.clone()}));}let mut params=args.trim_end_matches(')').split(',').filter(|x|!x.trim().is_empty()).map(|x|x.trim().to_string()).collect::<Vec<_>>();if params.first().map(|x|x.trim())!=Some("self"){params.insert(0,"self".into());}let(method_body,method_end)=indented(&body,j+1);funcs.insert(format!("{class_name}.{}",method_name.trim()),Rc::new(Function{params,body:method_body,closure:vars.clone()}));j=method_end;}else{j+=1;}}if let Some(parent_name)=parent{let prefix=format!("{parent_name}.");let inherited=funcs.iter().filter(|(name,_)|name.starts_with(&prefix)).map(|(name,function)|(name.trim_start_matches(&prefix).to_string(),function.clone())).collect::<Vec<_>>();for(name,function)in inherited{let child_name=format!("{class_name}.{name}");if !funcs.contains_key(&child_name){funcs.insert(child_name,function);}}}i=end;continue;}
    if let Some(rest)=line.strip_prefix("fn ").or_else(||line.strip_prefix("def ")){let head=rest.trim_end_matches(':');let(name,args)=head.split_once('(').ok_or("function syntax: fn name(a, b):".to_string())?;let args=args.trim_end_matches(')').split(',').filter(|x|!x.trim().is_empty()).map(|x|x.trim().to_string()).collect();let(body,end)=indented(lines,i+1);funcs.insert(name.trim().into(),Rc::new(Function{params:args,body,closure:vars.clone()}));i=end;continue;}
    if let Some(rest)=line.strip_prefix("return"){let v=if rest.trim().is_empty(){Value::None}else{expression(rest.trim(),vars,funcs)?};return Ok(Flow::Return(v));}
    if line=="break"{return Ok(Flow::Break);} if line=="continue"{return Ok(Flow::LoopContinue);}
    if let Some(c)=line.strip_prefix("while "){let condition=c.trim_end_matches(':').trim();let(body,end)=indented(lines,i+1);let mut guard=0;while expression(condition,vars,funcs)?.truthy(){match execute_lines(&body,vars,funcs,base)?{Flow::Return(v)=>return Ok(Flow::Return(v)),Flow::Break=>break,Flow::LoopContinue=>{},Flow::Continue=>{}}guard+=1;if guard>100000{return Err("loop limit exceeded".into());}}i=end;continue;}
    if let Some(rest)=line.strip_prefix("for "){let(name,src)=rest.trim_end_matches(':').split_once(" in ").ok_or("for syntax: for item in list:".to_string())?;let value=expression(src.trim(),vars,funcs)?;let(body,end)=indented(lines,i+1);match value{Value::List(items)=>for item in items{vars.insert(name.trim().into(),item);match execute_lines(&body,vars,funcs,base)?{Flow::Return(v)=>return Ok(Flow::Return(v)),Flow::Break=>break,Flow::LoopContinue=>continue,Flow::Continue=>{} }},_=>return Err("for expects a list".into())}i=end;continue;}
    if let Some(c)=line.strip_prefix("if "){let take=expression(c.trim_end_matches(':').trim(),vars,funcs)?.truthy();let(body,mut end)=indented(lines,i+1);if take{match execute_lines(&body,vars,funcs,base)?{Flow::Continue=>{},flow=>return Ok(flow)}}if end<lines.len()&&lines[end].trim()=="else:"{let(else_body,e)=indented(lines,end+1);if !take{match execute_lines(&else_body,vars,funcs,base)?{Flow::Continue=>{},flow=>return Ok(flow)}}end=e;}i=end;continue;}
    if let Some(x)=line.strip_prefix("say "){println!("{}",expression(x,vars,funcs)?.show());i+=1;continue;}
    if let Some(x)=line.strip_prefix("use "){let spec=x.trim();if spec.starts_with('"')||spec.contains('/')||spec.ends_with(".zp"){let raw=spec.trim_matches('"');let candidate=if Path::new(raw).extension().is_some(){raw.to_string()}else{format!("{raw}.zp")};let path=if Path::new(&candidate).is_absolute(){Path::new(&candidate).to_path_buf()}else{resolve_module(base,raw).ok_or(format!("module not found: {raw}"))?};let imported=fs::read_to_string(&path).map_err(|e|format!("cannot import {}: {e}",path.display()))?;let imported_lines=imported.lines().map(str::to_string).collect::<Vec<_>>();match execute_lines(&imported_lines,vars,funcs,base)?{Flow::Continue=>{},Flow::Return(_)=>return Err("return is not allowed at module top level".into()),flow=>return Ok(flow)}}else{println!("[Zap native] module declared: {spec}");}i+=1;continue;}
    if let Some(x)=line.strip_prefix("let "){let(n,v)=x.split_once('=').ok_or(format!("line {}: expected =",i+1))?;let(name,annotation)=n.trim().split_once(':').map(|(name,ty)|(name.trim(),Some(ty.trim()))).unwrap_or((n.trim(),None));let value=expression(v,vars,funcs)?;if let Some(ty)=annotation{check_annotation(name,ty,&value).map_err(|e|format!("line {}: {e}",i+1))?;}vars.insert(name.into(),value);i+=1;continue;}
    if !line.contains("==")&&!line.contains("!=")&&!line.contains("<=")&&!line.contains(">="){if let Some((n,v))=line.split_once('='){let target=n.trim();let value=expression(v,vars,funcs)?;if let Some((object_name,field))=target.split_once('.') {let object=vars.get_mut(object_name).ok_or(format!("undefined variable: {object_name}"))?;match object{Value::Object{fields,..}=>{fields.borrow_mut().insert(field.trim().into(),value);},_=>return Err("property assignment expects an object".into())}}else{vars.insert(target.into(),value);}i+=1;continue;}}
    let _ = expression(line,vars,funcs)?; i+=1; continue;}Ok(Flow::Continue)}

fn run(source:&str,base:&Path)->Result<(),String>{let mut vars=HashMap::new();let mut funcs=HashMap::new();let lines=source.lines().map(str::to_string).collect::<Vec<_>>();match execute_lines(&lines,&mut vars,&mut funcs,base)?{Flow::Continue|Flow::Return(_)=>Ok(()),Flow::Break|Flow::LoopContinue=>Err("break/continue must be inside a loop".into())}}
fn format_source(source:&str)->String{let mut out=String::new();for line in source.lines(){let normalized=line.replace('\t',"    ").trim_end().to_string();out.push_str(&normalized);out.push('\n');}out}
fn manifest_value(text:&str,key:&str)->Option<String>{for line in text.lines(){let line=line.trim();if line.starts_with('#')||!line.starts_with(key){continue;}if let Some((k,v))=line.split_once('='){if k.trim()==key{return Some(v.trim().trim_matches('"').to_string());}}}None}
fn validate_project(dir:&Path)->Result<String,String>{let manifest=dir.join("zap.toml");let text=fs::read_to_string(&manifest).map_err(|e|format!("cannot read {}: {e}",manifest.display()))?;let name=manifest_value(&text,"name").ok_or("zap.toml: missing package name".to_string())?;let version=manifest_value(&text,"version").ok_or("zap.toml: missing package version".to_string())?;let main=manifest_value(&text,"main").unwrap_or_else(||"main.zp".into());if !dir.join(&main).is_file(){return Err(format!("zap.toml: main file not found: {}",dir.join(&main).display()));}Ok(format!("{name} {version} (main: {main})"))}
fn resolve_module(base:&Path,raw:&str)->Option<PathBuf>{let candidate=if Path::new(raw).extension().is_some(){raw.to_string()}else{format!("{raw}.zp")};let candidates=[base.join(&candidate),base.join("modules").join(&candidate),base.join("lib").join(&candidate)];candidates.into_iter().find(|p|p.is_file())}
fn collect_test_files(dir:&Path,files:&mut Vec<PathBuf>)->Result<(),String>{for entry in fs::read_dir(dir).map_err(|e|format!("cannot read test directory {}: {e}",dir.display()))?{let path=entry.map_err(|e|format!("cannot inspect test directory {}: {e}",dir.display()))?.path();if path.is_dir(){collect_test_files(&path,files)?;}else if path.extension().and_then(|x|x.to_str())==Some("zp")&&path.file_stem().and_then(|x|x.to_str()).map(|x|x.ends_with("_test")).unwrap_or(false){files.push(path);}}Ok(())}
fn run_zap_tests(dir:&Path)->Result<usize,String>{let mut files=Vec::new();collect_test_files(dir,&mut files)?;files.sort();if files.is_empty(){return Err(format!("no *_test.zp files found in {}",dir.display()));}let mut passed=0;for path in files{let source=fs::read_to_string(&path).map_err(|e|format!("cannot read {}: {e}",path.display()))?;match run(&source,path.parent().unwrap_or(Path::new("."))){Ok(())=>{println!("ok   {}",path.display());passed+=1;},Err(error)=>{eprintln!("FAIL {}: {}",path.display(),error);return Err(format!("{} test file(s) failed",path.display()));}}}println!("{} Zap test file(s) passed",passed);Ok(passed)}
fn json_escape(value:&str)->String{value.replace('\\',"\\\\").replace('"',"\\\"").replace('\n',"\\n").replace('\r',"\\r")}
fn lint_source(source:&str)->Vec<String>{let mut issues=Vec::new();for (index,line) in source.lines().enumerate(){let number=index+1;if line.contains('\t'){issues.push(format!("line {number}: tabs are not allowed; use spaces"));}if line.trim_end()!=line{issues.push(format!("line {number}: trailing whitespace"));}if line.len()>120{issues.push(format!("line {number}: line exceeds 120 characters"));}}issues}
fn print_project_json(dir:&Path){match validate_project(dir){Ok(info)=>println!("{{\"ok\":true,\"project\":\"{}\"}}",json_escape(&info)),Err(error)=>{println!("{{\"ok\":false,\"error\":\"{}\"}}",json_escape(&error));process::exit(1);}}}
fn main(){let args:Vec<String>=env::args().collect();if args.len()==2&&(args[1]=="--version"||args[1]=="-V"){println!("zap 0.8.0 (native)");return;}if args.len()==2&&(args[1]=="--help"||args[1]=="-h"){println!("Zap native runtime\n\nUsage:\n  zap <file.zp>       Run a Zap source file\n  zap run <file.zp>   Run a Zap source file explicitly\n  zap fmt <file.zp>   Format a Zap source file\n  zap check [dir]      Validate zap.toml and the project entry file\n  zap test [dir]       Run *_test.zp files in a tests directory
  zap lint <file.zp>   Check formatting and style warnings
  zap check --json     Validate a project with JSON diagnostics\n  zap build [dir]      Validate and prepare a Zap project\n  zap init <dir>       Create a new Zap project\n  zap --version        Show the version\n  zap --help           Show this help");return;}if args.len()==3&&args[1]=="init"{let dir=Path::new(&args[2]);if dir.exists(){eprintln!("cannot initialize existing path: {}",dir.display());process::exit(1);}fs::create_dir_all(dir).unwrap_or_else(|e|{eprintln!("cannot create {}: {e}",dir.display());process::exit(1);});fs::write(dir.join("zap.toml"),"[package]\nname = \"hello-zap\"\nversion = \"0.1.0\"\nmain = \"main.zp\"\n").unwrap_or_else(|e|{eprintln!("cannot write manifest: {e}");process::exit(1);});fs::write(dir.join("main.zp"),"fn main():\n    say \"Hello from Zap\"\n\nmain()\n").unwrap_or_else(|e|{eprintln!("cannot write entry file: {e}");process::exit(1);});fs::create_dir_all(dir.join("tests")).unwrap_or_else(|e|{eprintln!("cannot create test directory: {e}");process::exit(1);});fs::write(dir.join("tests").join("smoke_test.zp"),"let total = 2 + 3\nassert(total == 5, \"basic arithmetic failed\")\nsay \"smoke test passed\"\n").unwrap_or_else(|e|{eprintln!("cannot write starter test: {e}");process::exit(1);});println!("Created Zap project: {}",dir.display());return;}if args.len()==2&&args[1]=="check"{let dir=Path::new(".");match validate_project(dir){Ok(info)=>println!("valid Zap project: {info}"),Err(e)=>{eprintln!("Zap check error: {e}");process::exit(1);}}return;}if args.len()==3&&args[1]=="check"{if args[2]=="--json"{print_project_json(Path::new("."));return;}let dir=Path::new(&args[2]);match validate_project(dir){Ok(info)=>println!("valid Zap project: {info}"),Err(e)=>{eprintln!("Zap check error: {e}");process::exit(1);}}return;}if args.len()==4&&args[1]=="check"&&args[2]=="--json"{print_project_json(Path::new(&args[3]));return;}if args.len()==2&&args[1]=="test"{if let Err(e)=run_zap_tests(Path::new("tests")){eprintln!("Zap test error: {e}");process::exit(1);}return;}if args.len()==2&&args[1]=="build"{match validate_project(Path::new(".")){Ok(info)=>println!("built Zap project: {info}"),Err(e)=>{eprintln!("Zap build error: {e}");process::exit(1);}}return;}if args.len()==3&&args[1]=="build"{let dir=Path::new(&args[2]);match validate_project(dir){Ok(info)=>println!("built Zap project: {info}"),Err(e)=>{eprintln!("Zap build error: {e}");process::exit(1);}}return;}if args.len()==3&&args[1]=="test"{if let Err(e)=run_zap_tests(Path::new(&args[2])){eprintln!("Zap test error: {e}");process::exit(1);}return;}if args.len()==3&&args[1]=="run"{let source=fs::read_to_string(&args[2]).unwrap_or_else(|e|{eprintln!("{e}");process::exit(1);});let base=Path::new(&args[2]).parent().unwrap_or(Path::new("."));if let Err(e)=run(&source,base){eprintln!("Zap error: {e}");process::exit(1);}return;}if args.len()==3&&args[1]=="lint"{let path=Path::new(&args[2]);let source=fs::read_to_string(path).unwrap_or_else(|e|{eprintln!("cannot read {}: {e}",path.display());process::exit(1);});let issues=lint_source(&source);if issues.is_empty(){println!("lint ok: {}",path.display());}else{for issue in issues{println!("{}: {}",path.display(),issue);}process::exit(1);}return;}if args.len()==3&&args[1]=="fmt"{let path=Path::new(&args[2]);let source=fs::read_to_string(path).unwrap_or_else(|e|{eprintln!("cannot read {}: {e}",path.display());process::exit(1);});fs::write(path,format_source(&source)).unwrap_or_else(|e|{eprintln!("cannot write {}: {e}",path.display());process::exit(1);});return;}if args.len()!=2{eprintln!("Usage: zap <file.zp>\n       zap run <file.zp>\n       zap fmt <file.zp>\n       zap lint <file.zp>\n       zap check [dir]\n       zap check --json [dir]\n       zap test [dir]\n       zap build [dir]\n       zap init <dir>\n       zap --version");process::exit(2);}let source=fs::read_to_string(&args[1]).unwrap_or_else(|e|{eprintln!("{e}");process::exit(1);});let base=Path::new(&args[1]).parent().unwrap_or(Path::new("."));if let Err(e)=run(&source,base){eprintln!("Zap error: {e}");process::exit(1);}}

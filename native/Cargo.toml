[package]
name = "zap-native"
version = "2.0.0"
edition = "2021"
description = "Standalone native runtime for Zap"

[[bin]]
name = "zap"
path = "src/main.rs"

[dependencies]
serde_json = "1"
sha2 = "0.10"
ureq = { version = "=2.9.7", default-features = false, features = ["tls"] }

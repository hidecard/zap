export fn test():
    let lines = ["a", "b", "c"]
    let cursor = 0
    while cursor < len(lines):
        let x = lines[cursor]
        cursor = cursor + 1
        let y = 1
    say "done"
    return none

test()

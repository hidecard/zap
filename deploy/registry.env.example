# Copy to /etc/zap/registry.env with mode 0600; never commit the populated file.
# Generate both values through the deployment secret manager.
ZAP_REGISTRY_TOKEN=replace-with-secret-manager-token
ZAP_REGISTRY_SIGNING_SECRET=replace-with-secret-manager-signing-secret

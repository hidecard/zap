export fn test():
    let lines = ["a", "b", "c"]
    let cursor = 0
    while cursor < len(lines):
        let x = lines[cursor]
        if cursor == 2:
            break
        cursor = cursor + 1
    say "done"
    return none

test()

use json
use ai

fn greet(name):
    return "Hello, " + name

map user = {"name": "Zap", "version": 2}
say greet(user["name"])

count = 0
while count < 3:
    say count
    count = count + 1

payload = json.stringify user
say payload
answer = ai.ask "What is Zap?"
say answer["text"]

export fn test():
    let lines = ["a", "b", "c"]
    let cursor = 0
    while cursor < len(lines):
        let x = lines[cursor]
        cursor = cursor + 1
        if cursor == 3:
            say "about to exit loop"
    say "done"
    return none

test()
